<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Logo Manager</title>
  <style>
    /* Reset some default styles */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    /* Body with gradient background and centered content */
    body {
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
    }

    /* Container for smooth, cohesive look */
    .container {
      background: #f0f0f0;
      padding: 40px 30px;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
      max-width: 950px;
      width: 90%;
      transition: all 0.3s ease;
    }

    /* Headings */
    h1 {
      text-align: center;
      margin-bottom: 20px;
      font-size: 2.2rem;
      color: #333;
    }

    h2 {
      margin-top: 30px;
      margin-bottom: 10px;
      font-size: 1.5rem;
      color: #555;
      text-align: center;
    }

    /* Form styles */
    form {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
      flex-wrap: wrap;
      justify-content: center;
    }
    input[type="text"] {
      flex: 1;
      min-width: 200px;
      padding: 12px 15px;
      border: 2px solid #ccc;
      border-radius: 10px;
      font-size: 1rem;
      background: #fff;
      transition: border-color 0.3s, box-shadow 0.3s;
    }
    input[type="text"]:focus {
      border-color: #007bff;
      box-shadow: 0 0 8px rgba(0,123,255,0.3);
      outline: none;
    }
    button {
      padding: 12px 20px;
      border: none;
      border-radius: 10px;
      background-color: #007bff;
      color: #fff;
      font-size: 1.1rem;
      cursor: pointer;
      transition: all 0.3s;
    }
    button:hover {
      background-color: #0056b3;
      transform: translateY(-2px);
    }

    /* Table styles with matching borders */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      font-size: 0.95rem;
      border: 2px solid #ccc; /* Outer border for table */
    }
    th, td {
      padding: 15px;
      border: 2px solid #ccc; /* Match table border */
      text-align: left;
      background: #fff;
    }
    th {
      background-color: #e9ecef;
      font-weight: 600;
    }

    /* Input displaying URL - match border style */
    input[type="text"] {
      border: 2px solid #ccc;
      border-radius: 8px;
      background: #fff;
    }

    /* Actions cell layout and matching borders */
    td.actions {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      min-width: 180px;
    }

    /* Both buttons styled with matching borders and rounded corners */
    .actions button {
      padding: 8px 12px;
      border-radius: 8px;
      border: 2px solid #007bff;
      background-color: #007bff;
      color: #fff;
      font-size: 0.9rem;
      cursor: pointer;
      transition: all 0.2s;
    }
    .actions button:hover {
      background-color: #0056b3;
      border-color: #0056b3;
      transform: translateY(-2px);
    }

    /* View Logs styled as a button for consistency */
    .log-btn {
      display: inline-block;
      padding: 8px 12px;
      border-radius: 8px;
      border: 2px solid #28a745;
      background-color: #28a745;
      color: #fff;
      font-size: 0.9rem;
      text-decoration: none;
      transition: all 0.2s;
    }
    .log-btn:hover {
      background-color: #218838;
      border-color: #218838;
      transform: translateY(-2px);
    }

    /* Responsive adjustments */
    @media(max-width: 600px){
      form {
        flex-direction: column;
        align-items: center;
      }
      button {
        width: 100%;
      }
      table {
        font-size: 0.9rem;
      }
      td.actions {
        flex-direction: column;
        min-width: auto;
        gap: 8px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Logo Manager - Dashboard</h1>

    <!-- Create new logo -->
    <h2>Create a New Logo</h2>
    <form action="/create" method="POST">
      <input type="text" id="name" name="name" placeholder="Logo Name (optional)" />
      <button type="submit">Create Logo</button>
    </form>

    <hr />

    <!-- List existing logos -->
    <h2>Existing Logos</h2>
    <% if (pixels.length === 0) { %>
      <p style="text-align:center; margin-top:20px;">No logos created yet.</p>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Created</th>
            <th>Logo URL</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% 
          const months = ["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"];
          function formatDate(isoString) {
            const d = new Date(isoString);
            let day = d.getDate();
            let mon = months[d.getMonth()];
            let year = d.getFullYear();
            let hours = d.getHours();
            let minutes = d.getMinutes();
            let seconds = d.getSeconds();
            let ampm = hours >= 12 ? "pm" : "am";
            hours = hours % 12 || 12;
            const pad = (n) => n.toString().padStart(2, "0");
            return `${pad(day)}-${mon}-${year} ${pad(hours)}:${pad(minutes)}:${pad(seconds)}${ampm}`;
          }

          pixels.forEach(pixel => { 
          %>
            <tr>
              <td><%= pixel.name %></td>
              <td><%= formatDate(pixel.createdAt) %></td>
              <td>
                <input type="text" readonly value="<%= baseUrl + '/logo/' + pixel.id + '.png' %>" />
              </td>
              <td class="actions">
                <button class="actions copy-btn" onclick="copyToClipboard('<%= baseUrl + '/logo/' + pixel.id + '.png' %>')">Copy URL</button>
                <a href="/logs/<%= pixel.id %>" class="log-btn">View Logs</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </div>

  <script>
    function copyToClipboard(text) {
      navigator.clipboard.writeText(text).then(() => {
        alert("Copied to clipboard!");
      });
    }
  </script>
</body>
</html>
